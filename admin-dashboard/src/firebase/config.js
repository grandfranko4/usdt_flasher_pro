// This is a dummy file to avoid breaking imports in other files
// We're now using SQLite instead of Firebase

// Export empty objects
export const auth = {};
export const db = {};
export const storage = {};
